


# On windows the path uses \.
# You need to use double backslash in filepath as single \ followed by a character has diffferent properties in windows.
# You can also add r before the string ( r'c:\user\' ) to tell the interpreter that it is a raw string and not to format it. 