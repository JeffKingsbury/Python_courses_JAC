# 5) Write a program that prompts for a string and then prints the number of letters in the string (how many
# letters) and then prints all the letters in the string (one per line) (use a for loop)

str = input('Enter a string: ');
for x in str:
    print(x);